- name: playbook installing "{{ component }}"
  hosts: "{{ component }}"
  become: yes
  roles:
   - "{{ component }}"
  with_items:                         
    - "mysql"
    - "backend"
    - "frontend"