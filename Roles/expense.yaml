- name: Install components
  become: yes
  tasks:
    - name: Include roles for each component
      hosts: "{{ item }}"
      include_role:
        name: "{{ item }}"
      loop:
        - mysql
        - backend
        - frontend
