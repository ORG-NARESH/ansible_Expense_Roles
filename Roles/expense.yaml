- name: Install components
  hosts: all
  become: yes
  tasks:
    - name: Include roles for each component
      include_role:
        name: "{{ item }}"
      loop:
        - mysql
        - backend
        - frontend
      
